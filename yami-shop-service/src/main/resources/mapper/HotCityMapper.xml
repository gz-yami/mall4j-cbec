<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yami.shop.dao.HotCityMapper">

    <select id="listHotCity" resultType="com.yami.shop.bean.model.HotCity">
        SELECT `area_id`, `area_name`, `status`,`area_name_en`
        FROM tz_hot_city
        WHERE `status` = 1
        ORDER BY seq DESC, create_time DESC
    </select>
</mapper>
